# Static IntakePilot Demo

This folder contains a browser-only demo for the AI Intake Agent project.

Open `index.html` directly in a browser, or enable GitHub Pages and point Pages to the `/demo` folder if you want a simple portfolio demo URL.

The production-style FastAPI app remains in the main repository. This static demo is only for showcasing the clinic/legal intake workflow without running a backend.
